import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

const StyledWrapper = styled.div`
  width: 100%;
  padding: 2rem;
`;

const LoggedInSummary = () => {
  return (
    <StyledWrapper>
      <h1>hello</h1>
    </StyledWrapper>
  );
};

export default LoggedInSummary;
